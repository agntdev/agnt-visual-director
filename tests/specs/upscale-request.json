[
  {
    "name": "Upscale to 4K explains when no image is available",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "There’s no generated image to upscale yet. Create an image first." }
          }
        ],
        "send": {
          "callback": "upscale:request"
        }
      }
    ]
  }
]
